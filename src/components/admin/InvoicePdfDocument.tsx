
import React from 'react';
import { formatPrice } from '@/lib/utils';

// Simple stub component since we're using jsPDF for PDF generation
const InvoicePdfDocument = ({ invoice, order, companyInfo }: any) => {
  // This component is not needed with jsPDF approach
  return null;
};

export default InvoicePdfDocument;
