
export const htmlToPdfAndUpload = async (html: string, fileName: string): Promise<string> => {
  // Stub function for compatibility
  console.log('htmlToPdfAndUpload called', { html, fileName });
  return 'mock-pdf-url';
};

export default htmlToPdfAndUpload;
